# Repository Guidelines

- Keep the main branch in a clean, buildable state (`npm run dev` and `npx tsc --noEmit` should pass clean).
- Ensure all components and routes use responsive design and consistent typography.
- Preserve existing data schemas and API contracts when extending features.
